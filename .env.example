# ---- ElevenLabs (Scribe v2 transcription) ----
ELEVENLABS_API_KEY=
# Override only if ElevenLabs renames the model
ELEVENLABS_STT_MODEL=scribe_v2

# ---- OpenAI (segmentation + search reasoning only; never transcription) ----
OPENAI_API_KEY=
OPENAI_SEGMENT_MODEL=gpt-4.1
OPENAI_SEARCH_MODEL=gpt-4.1-mini

# ---- Supabase (anon key only — NEVER the service-role key) ----
SUPABASE_URL=https://vofogqdxxmjoqdfcsgen.supabase.co
SUPABASE_ANON_KEY=

# ---- Pipeline identity: a vault OWNER or a vault_members row with role='editor' ----
PIPELINE_EDITOR_EMAIL=
PIPELINE_EDITOR_PASSWORD=
